// Your "Output" tab should look like this

// Inner‑Self core (by LewdLeah)
InnerSelf("output");

// QETEco economic add‑on
QETEco("output");

// Minimal pass‑through modifier (LewdLeah baseline)
const modifier = (text) => {
  return { text };
};
modifier(text);
